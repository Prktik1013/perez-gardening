# Critique Report (impeccable + ReviveForge enforced pass)

**Reviewer:** ReviveForge impeccable/taste structured critique (pre-gate enforced)
**Verdict:** Fix before ship

## Taste dials in effect
- DESIGN_VARIANCE: medium — trust-first evidence, stable rhythm; diagonal-flow per DNA (signature grotesque diagonal local drift blueprint moss)
- MOTION_INTENSITY: low — urgent caller; subtle parallax + fade-ups only
- VISUAL_DENSITY: medium — balanced proof + services + facts

## Keep
- Real Google Maps photos for hero + before/after gallery (evidence-bound per BRIEF/PRODUCT; no stock)
- Exact phone (714) 932-3724 in multiple tap-to-call CTAs (header/hero/trust/sticky/footer); mobile-first
- Hero visual anchor: photo + overlay + diagonal shear + locality + coords (DNA-aligned)
- Blueprint grid + annotate labels + thin lines (signature detail)
- Earthy moss/stone palette (from-their-truck); sharp 2px radii (avoids rounded slop)
- Direct copy: "Grounds kept right." "Three ways we show up on site."
- Low-motion purposeful reveals matching dials

## Fix
- Fonts: Google-hosted Bricolage Grotesque + IBM Plex Sans. Violates DESIGN.md (premium self-hosted), PRODUCT.md (no Google leaks), visual-layer.
- Nested cards: .services__grid + .service-card (gaps, uniform padding/hover). Forbidden (anti-slop; no nested cards per PRODUCT/visual-layer).
- Masked tel: links (e.g. href="tel:+171****3724"). Production blocker.
- Missing DESIGN-AESTHETIC.md (incomplete contract per DESIGN-BRIEF).
- Contrast/hierarchy: 0.75/0.65/0.45 opacity text weakens readability.
- Gallery pairs + rotating divider adds noise vs clean evidence.
- Motion: vanilla JS (no GSAP ScrollTrigger per visual-layer).

## Quick wins
- Self-host fonts (@font-face + local); update CSS.
- Reframe services as non-card hyperframe (flush-left, no cards per DNA).
- Fix all tel: to full +17149323724; test.
- Create DESIGN-AESTHETIC.md citing peers (refero/land-book/squarespace).
- Tighten contrast; simplify gallery.
- Re-run impeccable + polish; pass reviveforge-automatic-qa checks.